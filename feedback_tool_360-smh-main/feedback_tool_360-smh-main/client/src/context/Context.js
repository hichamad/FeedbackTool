import { createContext } from "react";

// Context's made so that child components can acces the state of the parent component
export const QuestionListContext = createContext();
export const QuestionAnswerListContext = createContext();
